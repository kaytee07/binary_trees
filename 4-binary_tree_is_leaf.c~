#include "binary_trees.h"

/**
 * binary_tree_is_leaf - check if a node has no children
 * Return: 1 if node is a leaf and 0 if it isn't
 */


int binary_tree_is_leaf(const binary_tree_t *node)
{
if (node == NULL)
return (0);

if (node->left || node->right)
return (0);
else
return (1);
}
